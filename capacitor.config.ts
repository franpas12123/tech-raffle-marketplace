import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.website.rapol',
  appName: 'rapol-website',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
